name: Format PR6

on:
  push:
    branches: ['feat/durable-match-persistence']
    paths:
      - .github/workflows/format-pr6.yml

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 24
      - run: npm install --global --allow-scripts=pnpm pnpm@12.4.1
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec prettier --write apps/server/src/match-persistence.ts apps/server/test/postgres-match-store.test.ts
      - name: Commit formatting
        run: |
          if git diff --quiet; then
            echo 'Formatting already clean.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add apps/server/src/match-persistence.ts apps/server/test/postgres-match-store.test.ts
          git commit -m 'style(server): apply Prettier to persistence files'
          git push

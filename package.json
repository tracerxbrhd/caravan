{
  "name": "caravan",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@12.4.1",
  "engines": {
    "node": ">=24 <25",
    "pnpm": ">=12 <13"
  },
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier apps/miniapp/src/table.css && exit 1",
    "typecheck": "pnpm -r --if-present run typecheck",
    "test": "vitest run --passWithNoTests",
    "test:ci": "vitest run --passWithNoTests --no-file-parallelism",
    "verify": "pnpm build && pnpm lint && pnpm format:check && pnpm typecheck && pnpm test:ci",
    "dev:miniapp": "pnpm --filter @caravan/miniapp dev",
    "start:server": "pnpm --filter @caravan/server start",
    "start:bot": "pnpm --filter @caravan/bot start"
  },
  "devDependencies": {
    "@types/node": "24.13.4",
    "eslint": "10.10.0",
    "fast-check": "4.10.0",
    "prettier": "3.9.6",
    "typescript": "5.9.3",
    "typescript-eslint": "8.70.0",
    "vitest": "4.1.11"
  }
}
